// Root React component for the Mission #11 bookstore UI.
// Renders the main `BookList` catalog component.
import React from 'react';
import BookList from './components/BookList';

export default function App() {
  return <BookList />;
}

